import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import { ThemeProvider } from '@/components/theme-provider'
import './globals.css'

const _geist = Geist({ subsets: ["latin"] });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: 'Andrew Eliezer Tarigan | Software Engineer',
  description: 'I’m Andrew Eliezer Tarigan, usually just called Andrew. A curious and driven software engineer. With a background in Informatics, I have a strong interest in building and understanding scalable systems. I’m particularly drawn to back-end development, mobile apps, and UI/UX design. I enjoy the process of coding, debugging, and solving problems under pressure. For me, technology is a space to explore, create, and continuously grow.',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="scroll-smooth" suppressHydrationWarning>
      <body className="font-sans antialiased">
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem
          disableTransitionOnChange
        >
          {children}
        </ThemeProvider>
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
